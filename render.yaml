services:
  - type: web
    name: yt-summary-python-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn yt:app --host 0.0.0.0 --port $PORT
    plan: free
    healthCheckPath: /health
